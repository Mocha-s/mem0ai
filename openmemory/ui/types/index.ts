// Re-export all types from the main lib/types system
export * from '@/lib/types';

// Legacy exports for backward compatibility
export type { BaseEntity, ApiResponse, PaginatedResponse, ApiError } from '@/lib/types';
