# Mem0 MCP Server Requirements

# Core MCP dependencies
jsonrpc-server>=1.0.0
pydantic>=2.0.0
typing-extensions>=4.0.0

# HTTP transport for Streamable HTTP
aiohttp>=3.9.0
aiohttp-sse>=2.1.0

# Mem0 API client (for local server communication)
httpx>=0.25.0

# HTTP transport (legacy - keeping for compatibility)
fastapi>=0.100.0
uvicorn>=0.20.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Logging and monitoring
structlog>=23.0.0
opentelemetry-api>=1.15.0

# Security
cryptography>=41.0.0

# Environment configuration
python-dotenv>=1.0.0